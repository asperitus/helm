apiVersion: v1
kind: Service
metadata:
  name: {{ include "btcstore.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "btcstore.name" . }}
    helm.sh/chart: {{ include "btcstore.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: ssh
      protocol: TCP
      name: ssh
  selector:
    app.kubernetes.io/name: {{ include "btcstore.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
